# Ignore large trained model files (regenerated by running notebooks 01-03)
models/*.pkl
models/*.keras
models/*.json

# Keep the serialized train/val/test splits (small, required for reproducible evaluation)
!models/TestSets*/

# Ignore the dataset (download separately - see README)
data/EVSE-B-PowerCombined.csv

# MLflow tracking artifacts
mlruns/
mlartifacts/

# Result CSVs generated by evaluation cells (optional: remove these lines to version them)
df_production_results*.csv
bootstrap_fast_metrics.csv

# System / environment files
.ipynb_checkpoints/
__pycache__/
.DS_Store
evdetect_env/
mlflow_env/

# Additional Python and editor files
*.pyc
.env
.venv/
venv/
.idea/
.vscode/
*.log